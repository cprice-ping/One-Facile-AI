apiVersion: v1
kind: Service
metadata:
  name: mcp-admin-agent
spec:
  selector:
    app: mcp-admin-agent
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
